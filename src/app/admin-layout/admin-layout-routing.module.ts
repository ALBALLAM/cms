import {ModuleWithProviders} from '@angular/core';
import {Routes, RouterModule} from '@angular/router';

const routes: Routes = [
];

export const AdminLayoutRouting: ModuleWithProviders = RouterModule.forChild(routes);
